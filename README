pyfeed
-------

It converts RSS or Atom feeds from XML to a comma-separated file.

Setup
---

Create virtual env

$ python -m venv v_env
$ source v_env/bin/active
$ pip install -r requirements.txt

Usage
-----

Initial setup:

    mkdir -p "$HOME/.pyfeed/"
    cp pyfeedrc.example "$HOME/.pyfeed/pyfeedrc"

Edit the pyfeedrc configuration file and change any RSS/Atom feeds. 
This file is consumed as a python dictionary for pyfeed_update.

Update feeds
$ python pyfeed_update.py

View feeds
$ python pyfeed.py

